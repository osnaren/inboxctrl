import { ExtensionFeatureNotice } from '@/components/extension-feature-notice';

export default function IntelPage() {
  return (
    <ExtensionFeatureNotice
      title="Sender intelligence is an optional extension"
      description="The public app keeps the Gmail sync and action workflow focused. Advanced sender analytics can be registered by an optional extension package."
    />
  );
}
