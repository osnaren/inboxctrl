import { ExtensionFeatureNotice } from '@/components/extension-feature-notice';

export default function PlannerPage() {
  return (
    <ExtensionFeatureNotice
      title="AI label planner is an optional extension"
      description="The public app keeps AI-assisted triage, natural-language filter drafts, and dry-runs focused. Advanced label-tree planning can be loaded through an optional extension package."
    />
  );
}
